
# team-management files
/.claude/
.venv-team-management/
.mcp.json
team-management/config.json
team-management/
CLAUDE*

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local machine config (personal inbox/library/db paths). Copy geosorter.example.toml
# to geosorter.toml and edit; the real file stays local and untracked.
geosorter.toml

# Local SQLite databases / runtime data
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Local-only real media fixtures (privacy: real GPS; size: large MP4s)
tests/fixtures/media/local/

# Frontend (B7): node deps + the on-demand SPA build output served by the backend
frontend/node_modules/
src/geosorter/webui/

# Stray local lockfile backups (e.g. created before a manual uv.lock edit)
uv.lock.bak

# graphify generated knowledge graph (rebuilt locally via `graphify update .`)
graphify-out/
